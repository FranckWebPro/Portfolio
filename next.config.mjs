/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.drupal.org",
      "upload.wikimedia.org",
      "logowik.com",
      "dailybrand.co.zw",
    ],
  },
};

export default nextConfig;
